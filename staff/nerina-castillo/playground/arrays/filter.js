console.info("TEST filter");

console.info("CASE filter from arrays");

var words = ["spray", "elite", "exuberant", "destruction", "present"];

var result = words.filter((word) => word.length > 6);

console.info(result);
