console.info('TEST SOME')

console.info('CASE some in array')

var arrays = new Array(1, 2, 3, 4, 5, 0)



var even = array.some(function (element) {
    return element < 0
})

console.assert(array instanceof Array, 'arrays is an Array')
console.assert(even === true, 'even is true')
