console.log('TEST filter ind arrays')

console.log('CASE filter')


const words = ['spray', 'elite', 'exuberant', 'destruction', 'present'];

const result = words.filter((word) => word.length > 6);

console.log(result);
["exuberant", "destruction", "present"]
