console.log('TEST pop an element to array')


console.log('CASE pop an element to array')


//.pop()se utiliza para extraer un valor del final de una matriz. Podemos almacenar este valor eliminado asignándolo a una variable. En otras palabras, .pop()elimina el último elemento de una matriz y devuelve ese elemento.

var colors = new Array

colors[0] = { cara: 'rojo', cuerpo: 'amarillo', pies: 'verde' }
colors[1] = { cara: 'lila', cuerpo: 'rosa', pies: 'nude' }
colors[2] = { cara: 'azul', cuerpo: 'celeste', pies: 'cian' }
colors.length = 3

colors.pop() // funcion pop que elimina el ultimo

console.log(colors)
console.log(colors.length) // se ejecuta normal para ver el resultado de la consola y la longitud.

colors.pop()

colors.log(colors)
console.log(colors.length)
//sigue eliminando la ultima fila